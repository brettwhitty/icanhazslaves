# local .env file (see mise.toml)
.env

# mise local config (private config goes here)
mise.local.toml

# private/ local work dir for incoming assets needing review
!private/
!private/README.md
!private/docs

# state/ hierarchy
# Ignore all session-specific noise (logs, ephemeral state)
state/session/*

# Explicitly track the global/ architectural state
!state/global/

# Explicitly track the hierarchy documentation
!state/GEMINI.md
state/session/
state/repos/
